#include <iostream>
#include <vector>
#include <utility>
#include <functional>
#include <algorithm>
using namespace std;
bool compare(pair<int,int> a,pair<int,int> b){
    return a.first<b.first;
}
int main(){
    int T;
    cin >> T;
    for(int i=0;i<T;i++){
        int n,m;
        cin >> n >> m;
        vector<int,int>a(n,0);
        vector<int,int>b(m,0);
    }
}