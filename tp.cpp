
int SumMax(vector<int>& nums,int m){
    int res=0;
    int n=nums.size();
    int sum=0;
    int maxNum=0;
    for(int i=0;i<n;i++){
        sum += nums[i];
        maxNum = max(max)
    }
    int div = sum/m;
    while(){
        int groups=0;
        int temp=0;
        for(int i=0;i<n;i++){
            temp += nums[i];
            if(temp>div) groups++;
            
        }
    }
}